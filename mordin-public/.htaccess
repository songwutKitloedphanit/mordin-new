RewriteEngine On
RewriteBase /

# ถ้าไฟล์หรือโฟลเดอร์มีอยู่จริง ไม่ต้อง rewrite
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# ส่งทุก request ไปยัง index.php
RewriteRule ^ index.php [QSA,L]
